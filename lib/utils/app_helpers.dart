// Classe contenant tous les helpers de l'application
 void log(String message) {
  print(message); // Or use a logging package
}
void debugLog(String message) {
  print(message); // Or use a logging package
}